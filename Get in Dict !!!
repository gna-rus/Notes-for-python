#Python: подсчет колличества букв в веденном тексте и перевод их в словарь, где ключи - это буквы:
def word_dict(word)
:
    d = {}
    for c in word:
        d[c] = d.get(c, 0) + 1
    return d

text = input()
word = input()
x = word_dict(text)[word]
print(word_dict(text))
print(x)
